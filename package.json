{
  "name": "cipher-puzzle-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "node setup.js",
    "db:setup": "powershell -ExecutionPolicy Bypass -File ./database/db-setup.ps1",
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.0"
  }
}
